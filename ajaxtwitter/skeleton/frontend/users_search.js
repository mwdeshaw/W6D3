const APIUtil = require('./api_util.js');

class UsersSearch {
  constructor($el) {
    debugger
    this.$ul = $($el.attr(`data-users-ul`));
    this.$input = $($el.attr(`data-input`));
    this.$el = $el;
  }

  // handleInput

}